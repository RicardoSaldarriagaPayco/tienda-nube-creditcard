{
  "name": "tienda-nube-creditcard",
  "version": "1.0.0",
  "description": "pago tarjeta de credito tiendanube",
  "main": "index.js",
  "workspaces": {
    "packages": [
      "api",
      "frontend"
    ]
  },
  "scripts": {
    "setup:api": "cd api && yarn install",
    "start:api": "yarn workspace api dev",
    "docker-start": "npm run start:api && npm run dev",
    "setup:frontend": "cd frontend && yarn install",
    "start:frontend": "yarn workspace frontend dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite-plugin-svgr": "^4.2.0"
  }
}
